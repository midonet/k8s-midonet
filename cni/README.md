# Container Networking Interface driver

MidoNet's CNI driver receives network plugging requests from Kubernetes CNI
plugin and binds one side of the veth pair to a port in the MidoNet virtual
topology.
